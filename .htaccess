<IfModule mod_rewrite.c>
# Activar RewriteEngine
    RewriteEngine on
    RewriteBase /allparts
    RewriteCond %{SCRIPT_FILENAME} !-d
    RewriteCond %{SCRIPT_FILENAME} !-f

    RewriteRule ^(.*)$ processor.php?pg=$1
    #RewriteRule ^(.*)$ inicio.html
</IfModule>